# ~/.config/doti3/mise/config.toml
# mise global runtime version configuration
# See: https://mise.jdx.dev
#
# Synced to ~/.config/mise/config.toml via deploy.sh
#
# Node: core backend (prebuilt binaries)
# PHP:  registry backend (vfox-php community binaries)

[tools]
php = "8.5"
node = "24"

[settings]
experimental = true
jobs = 4
disable_backends = ["asdf"]
